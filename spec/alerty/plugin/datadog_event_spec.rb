require 'spec_helper'

describe Alerty::Plugin::DatadogEvent do
  it 'has a version number' do
    expect(Alerty::Plugin::DatadogEvent::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
